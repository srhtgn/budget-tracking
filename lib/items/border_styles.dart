import 'package:flutter/material.dart';
import 'package:gelirgidertakip/items/colors.dart';

class BorderStyles{ //Border stilleri  //Border Styles
  final  BorderRadius textFieldRadius = BorderRadius.circular(20);
  final  BorderRadius alertDialogRadius20 = BorderRadius.circular(20);
  final  BorderRadius alertDialogRadius15 = BorderRadius.circular(15);
  final  BorderRadius showModalBottomSheetRadius = BorderRadius.vertical(top: Radius.circular(25));
  final  StadiumBorder stadiumBorder = StadiumBorder();
  final BorderRadius monthMenuRadius = BorderRadius.circular(10);
  final BorderRadius yearMenuRadius = BorderRadius.circular(10);
  final BorderRadius categoryColorBoxRadius = BorderRadius.circular(3);
  final BorderSide monthButtonsColor = BorderSide(color: ColorsItems().myPink);
}